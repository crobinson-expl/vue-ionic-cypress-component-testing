import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'vue-ionic-cypress-component-testing',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
